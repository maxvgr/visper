# Backend integration

Информация для backend-разработчиков по интеграции готовой frontend-вёрстки Visper с серверной частью.

Backend получает собранную вёрстку с готовыми HTML-страницами, CSS и JavaScript-файлами.

При интеграции желательно сохранять существующую HTML-структуру, классы и `data-*` атрибуты, так как они используются frontend-логикой и стилями.

---

# Формы

## Общая информация

Frontend уже отвечает за:

- клиентскую валидацию;
- обязательное согласие на обработку персональных данных;
- блокировку кнопки отправки до принятия обязательного согласия;
- отображение ошибок клиентской валидации;
- работу модальных окон;
- открытие окна успешной отправки формы.

Формы, подключённые к frontend-логике, имеют класс:

```html
<form class="form form-custom"></form>
```

Для форм с клиентской валидацией используется атрибут:

```html
data-validation
```

Поля, которые проверяются frontend, имеют:

```html
data-validate
```

Обязательные согласия имеют:

```html
data-privacy
```

Пока обязательный checkbox с `data-privacy` не отмечен, frontend блокирует кнопку отправки.

Backend должен повторно проверять обязательные поля и согласие пользователя независимо от клиентской проверки.

---

# Отправка форм

В production frontend самостоятельно заявку на сервер не отправляет.

Backend может использовать:

- обычную HTML-отправку;
- AJAX;
- API;
- отправку в CMS;
- отправку в CRM.

При обычной отправке можно добавить форме необходимые:

```html
action="/..." method="post"
```

При этом желательно не удалять существующие классы и `data-*` атрибуты формы.

На стороне backend также необходимо реализовать необходимые для проекта:

- серверную валидацию;
- CSRF-защиту;
- CAPTCHA / anti-spam;
- защиту от частых повторных запросов;
- отправку писем;
- передачу заявки в CRM / CMS;
- логирование ошибок.

Frontend-валидация не заменяет серверную.

---

# AJAX и успешная отправка

Если форма отправляется через AJAX, после получения от сервера подтверждения успешной обработки заявки необходимо вызвать:

```js
window.dispatchEvent(
  new CustomEvent("formSuccess", {
    detail: { form },
  }),
);
```

где `form` — DOM-элемент успешно отправленной формы.

Например:

```js
const form = document.querySelector(".form-custom");

fetch("/api/form/", {
  method: "POST",
  body: new FormData(form),
})
  .then((response) => {
    if (!response.ok) {
      throw new Error("Ошибка отправки формы");
    }

    return response.json();
  })
  .then(() => {
    window.dispatchEvent(
      new CustomEvent("formSuccess", {
        detail: { form },
      }),
    );
  });
```

Frontend после события самостоятельно закроет текущее модальное окно и откроет окно успешной отправки, указанное у формы:

```html
data-success-modal="success"
```

При необходимости имя окна можно передать явно:

```js
window.dispatchEvent(
  new CustomEvent("formSuccess", {
    detail: {
      form,
      successModal: "success",
    },
  }),
);
```

`formSuccess` необходимо вызывать только после подтверждения успешной обработки данных сервером.

Не вызывать событие сразу после начала HTTP-запроса.

---

# Ошибки при AJAX-отправке

Frontend автоматически выводит ошибки собственной клиентской валидации.

Автоматическая обработка серверных ошибок отдельных полей пока не предусмотрена.

Например, ответ:

```json
{
  "errors": {
    "user_email": "Некорректный адрес электронной почты"
  }
}
```

сам по себе не будет автоматически выведен около соответствующего поля.

Если для проекта потребуется вывод серверных ошибок непосредственно у полей, необходимо согласовать формат ответа и добавить соответствующую frontend-обработку.

---

# Текущие формы

## Форма «Напишите нам»

Поля формы:

```text
user_name
user_email
user_message
privacy
subscribe
```

Назначение:

- `user_name` — имя пользователя;
- `user_email` — email;
- `user_message` — сообщение;
- `privacy` — обязательное согласие на обработку персональных данных;
- `subscribe` — необязательное согласие на рекламно-информационные рассылки.

---

## Форма обратного звонка

Поля формы:

```text
user_name
user_phone
captcha
privacy
marketing
```

Назначение:

- `user_name` — имя пользователя;
- `user_phone` — телефон;
- `captcha` — CAPTCHA;
- `privacy` — обязательное согласие на обработку персональных данных;
- `marketing` — необязательное согласие на рекламно-информационные рассылки.

---

## Модальная форма «Напишите нам»

Поля формы:

```text
user_name
user_email
message
captcha
privacy
marketing
```

Назначение:

- `user_name` — имя пользователя;
- `user_email` — email;
- `message` — сообщение;
- `captcha` — CAPTCHA;
- `privacy` — обязательное согласие на обработку персональных данных;
- `marketing` — необязательное согласие на рекламно-информационные рассылки.

---

# Имена полей

На текущем этапе в разных формах есть отличающиеся названия одинаковых по смыслу полей:

```text
user_message / message
subscribe / marketing
```

При интеграции необходимо ориентироваться на фактический атрибут `name` поля в HTML конкретной формы.

Не переименовывать поля только на backend без соответствующего изменения HTML.

Если понадобится унификация имён, её необходимо согласовать с frontend.

---

# CAPTCHA

В собранной тестовой вёрстке CAPTCHA является заглушкой и не должна считаться production-защитой.

Настоящая CAPTCHA / anti-spam проверка должна выполняться сервером.

Backend может заменить тестовый механизм на используемую в production CAPTCHA, сохранив при необходимости существующую структуру формы.

После подключения реальной CAPTCHA тестовая заглушка может быть удалена.

---

# Yandex Maps API

## Общая информация

Секция «Контакты» использует Yandex Maps JavaScript API v3.

Frontend-код карты ожидает наличие глобального объекта:

```js
window.ymaps3;
```

Если API карты не подключён, блок просто не инициализируется.

Сам собранный JS карты API-ключ не создаёт и не получает.

---

## Текущая тестовая конфигурация

На этапе разработки используется бесплатный Yandex Maps API, настроенный для временного тестового домена проекта.

Это временная конфигурация.

Перед запуском production необходимо:

1. определить окончательный боевой домен;
2. проверить настройки API-ключа;
3. разрешить использование ключа на production-домене либо создать отдельный production-ключ;
4. проверить актуальные лимиты и условия Yandex Maps API;
5. проверить карту непосредственно на боевом домене.

Если оставить ограничения только на временный домен, после переноса сайта карта может перестать работать.

---

# Подключение Yandex Maps

Yandex Maps API должен быть подключён на странице до выполнения frontend-кода карты.

Пример:

```html
<script src="https://api-maps.yandex.ru/v3/?apikey=YANDEX_MAPS_API_KEY&lang=ru_RU"></script>
```

Способ хранения и подстановки ключа определяется backend / CMS проекта.

Ключ рекомендуется получать из конфигурации проекта, а не вручную прописывать в каждом шаблоне.

Следует учитывать, что ключ браузерного JavaScript API будет виден посетителю сайта через HTML / Network.

Поэтому ограничение использования ключа должно быть настроено в кабинете Yandex по разрешённым доменам.

---

# Настройки карты

Сейчас карта использует координаты офиса:

```text
30.3718, 59.9564
```

Если адрес компании изменится, необходимо одновременно проверить координаты карты.

Если контактная информация будет храниться в CMS, рекомендуется хранить отдельно:

```text
longitude
latitude
```

и передавать готовые координаты в frontend.

Получать координаты из текстового адреса непосредственно в браузере не требуется.

---

# Контактные данные

На текущем этапе в готовой вёрстке используются:

```text
195009, г. Санкт-Петербург, улица Комсомола, 1-3М
+7 921 642-78-58
slatinskiy@f-elres.ru
ПН-ПТ
09:00-17:00
```

Эти значения могут быть заменены данными из CMS.

При серверной генерации блока желательно сохранять существующую HTML-структуру и CSS-классы.

При изменении адреса также необходимо обновить координаты карты.

---

# Важно сохранить при интеграции

Frontend JavaScript использует следующие классы и атрибуты:

```text
.form-custom
data-validation
data-validate
data-privacy
data-success-modal
```

Не удалять и не переименовывать их без необходимости.

Также желательно сохранять существующие классы элементов форм и интерактивных блоков, так как они используются CSS и JavaScript.

Если backend существенно меняет HTML-структуру интерактивного компонента, после этого необходимо проверить работу frontend.

---

# Перед production

Необходимо проверить:

- адреса отправки всех форм;
- способ отправки форм;
- серверную валидацию;
- обязательное согласие `privacy`;
- CAPTCHA / anti-spam;
- CSRF-защиту;
- защиту от частых запросов;
- отправку писем / интеграцию с CRM;
- обработку ошибок;
- вызов `formSuccess` при успешной AJAX-отправке;
- production-ключ Yandex Maps API;
- разрешённые домены для API-ключа;
- актуальные ограничения и условия Yandex Maps;
- координаты офиса;
- контактные данные.


# Карта регионов поставки

В секции «Работаем по всей России» точки на карте уже расположены frontend-разработчиком.

Координаты существующих точек менять не нужно.

Пример пустой точки:

```html
<button class="about-regions__point" style="--point-x: 43.0244%; --point-y: 32%;" type="button" aria-label="Регион поставки"></button>
```

Чтобы добавить данные региону, необходимо использовать уже существующую точку с нужными координатами и добавить ей:

```text
data-region
data-value
data-label
```

Также необходимо заменить `aria-label` на название региона.

Пример заполненной точки:

```html
<button class="about-regions__point" style="--point-x: 62.5366%; --point-y: 77.8537%;" type="button" data-region="Амурская область" data-value="52" data-label="Вакуумных контактора" aria-label="Амурская область"></button>
```

Назначение данных:

- `data-region` — название региона;
- `data-value` — числовое значение;
- `data-label` — подпись к значению;
- `aria-label` — название региона для accessibility.

Frontend самостоятельно выводит эти данные во всплывающей подсказке при взаимодействии с точкой.

Не создавать новую точку путём копирования другой точки, если нужная позиция уже присутствует на карте.

Не изменять:

```text
class="about-regions__point"
--point-x
--point-y
```

без согласования с frontend, так как эти значения отвечают за расположение точки на карте.

Если количество регионов изменяется, необходимо также обновить текст счётчика секции, например:

```text
[ 12 регионов поставки ]
```

---

# Страница новости

Шаблон отдельной новости находится в:

```text
src/sp-news.html
```

Backend должен подставлять данные конкретной новости, сохраняя существующую HTML-структуру и CSS-классы.

Основные динамические данные:

- дата публикации;
- заголовок;
- главное изображение;
- содержимое новости;
- изображения внутри материала;
- количество просмотров;
- ссылки блока «Читайте также»;
- данные следующей новости.

## Дата и заголовок

Дата публикации выводится в:

```html
<time class="news-detail__date" datetime="2026-06-01">
  01.06.2026
</time>
```

В `datetime` передавать дату в формате `YYYY-MM-DD`, внутри элемента — в пользовательском формате `DD.MM.YYYY`.

Заголовок выводится в:

```html
<h1 class="news-detail__title">
  Заголовок новости
</h1>
```

Принудительные `<br>` в тестовой вёрстке используются только для соответствия макету. В production их добавлять только при необходимости.

## Главное изображение

Главное изображение выводится в существующей структуре:

```html
<div class="news-detail__hero cut-border cut-border--large">
  <picture class="lazy">
    <img class="image image--contain lazy__item" data-src="..." alt="...">
  </picture>
</div>
```

Backend подставляет `data-src` и `alt`.

Классы контейнера и изображения сохранять.

## Содержимое новости

Основной контент расположен внутри:

```html
<div class="news-detail__content">
```

Текущая тестовая новость разбита на смысловые секции:

```html
<section id="news-intro" class="news-detail__section">
```

Внутри могут использоваться заголовки, абзацы, списки и изображения.

Если структура новости формируется CMS динамически, желательно сохранять существующие классы элементов, которые должны использовать стили готовой вёрстки.

## Секции и боковая навигация

На desktop предусмотрена навигация по разделам новости:

```html
<aside class="news-detail__navigation">
```

Ссылки навигации должны соответствовать `id` секций:

```html
<a href="#news-features" class="news-detail__navigation-link">
  Особенности
</a>

<section id="news-features" class="news-detail__section news-detail__section--features">
```

Если конкретной секции в новости нет, соответствующий пункт навигации не выводить.

Если новость не имеет структурированных разделов, блок `.news-detail__navigation` можно не выводить полностью.

На планшетах и мобильных устройствах навигация скрывается frontend-стилями.

## Изображения внутри материала

Для медиа внутри новости используется контейнер:

```html
<div class="news-detail__media cut-border">
```

Изображение необходимо выводить через принятую в проекте lazy-структуру:

```html
<div class="news-detail__media cut-border">
  <picture class="lazy">
    <img class="image image--cover lazy__item" data-src="..." alt="...">
  </picture>
</div>
```

`image--cover` или `image--contain` выбирается в зависимости от типа изображения.

## Просмотры

Количество просмотров выводится в:

```html
<div class="news-detail__views">
  Просмотров: 1376
</div>
```

Backend подставляет актуальное значение.

Способ подсчёта просмотров определяется CMS / backend.

## Кнопка «Поделиться»

В шаблоне предусмотрена кнопка:

```html
<button class="news-detail__share" type="button">
```

HTML-структуру и классы кнопки сохранять.

Если production-механизм шаринга потребует дополнительной frontend-логики, его необходимо согласовать отдельно.

## Блок «Читайте также»

Связанные материалы выводятся в:

```html
<div class="news-detail__related">
```

Backend подставляет реальные названия и URL новостей:

```html
<ul class="news-detail__related-list">
  <li>
    <a href="/news/example/">Название новости</a>
  </li>
</ul>
```

Способ подбора связанных материалов определяется CMS.

Если связанных материалов нет, `.news-detail__related` можно не выводить.

---

# Последовательная и бесконечная загрузка новостей

После основного материала в текущем шаблоне расположен блок:

```html
<section class="news-next">
```

Он представляет следующую новость в последовательности.

Backend должен подставлять:

- дату следующей новости;
- заголовок;
- главное изображение;
- URL следующей новости.

Пример:

```html
<section class="news-next">
  <div class="container">
    <div class="section__line"></div>

    <time class="news-next__date" datetime="2026-06-05">
      05.06.2026
    </time>

    <h2 class="news-next__title">
      Заголовок следующей новости
    </h2>

    <a href="/news/next-news/" class="news-next__preview cut-border cut-border--large">
      <picture class="lazy">
        <img class="image image--contain lazy__item" data-src="..." alt="">
      </picture>
    </a>
  </div>
</section>
```

Новости должны образовывать последовательную цепочку:

```text
новость 1
→ новость 2
→ новость 3
→ ...
```

Для каждой открытой новости backend определяет следующую новость по принятому в CMS порядку сортировки.

На последней новости, если следующего материала нет, блок `.news-next` не выводить полностью.

Не использовать `href="#"` в production как обозначение отсутствия следующей новости.

## Важно по infinite scroll

Текущая frontend-вёрстка подготавливает HTML-структуру следующей новости, но автоматическая AJAX / infinite-scroll подгрузка сейчас frontend JavaScript не реализована.

Если по требованиям production следующая новость должна автоматически подгружаться при прокрутке без обычного перехода на новую страницу, механизм необходимо реализовать отдельно.

До реализации необходимо согласовать с frontend:

- URL / endpoint получения следующей новости;
- возвращается полная HTML-страница или только HTML-фрагмент новости;
- какой фрагмент добавляется в DOM;
- как передаётся URL следующей новости;
- нужно ли менять URL браузера через History API;
- как должны работать Back / Forward;
- когда засчитывается просмотр автоматически подгруженной новости;
- как обрабатываются ошибки загрузки;
- как определяется конец списка.

Не менять существующую структуру `.news-detail` и `.news-next` без согласования с frontend, так как она используется стилями страницы.

Даже при бесконечной загрузке каждая новость должна оставаться доступной по собственному постоянному URL для прямого перехода и SEO.

## Важно сохранить для страницы новости

Без согласования с frontend не переименовывать основные классы:

```text
.news-detail
.news-detail__header
.news-detail__date
.news-detail__title
.news-detail__hero
.news-detail__layout
.news-detail__content
.news-detail__section
.news-detail__heading
.news-detail__media
.news-detail__list
.news-detail__meta
.news-detail__share
.news-detail__views
.news-detail__related
.news-detail__navigation
.news-detail__navigation-link
.news-next
.news-next__date
.news-next__title
.news-next__preview
```

Также сохранять используемые служебные классы:

```text
.cut-border
.cut-border--large
.lazy
.lazy__item
.image
.image--contain
.image--cover
```
